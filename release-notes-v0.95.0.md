# Rembry v0.95.0 稳定版

本版本完成 Windows 端稳定化与宽屏体验收口，同时保留已经通过实际使用检验的手机端功能和数据结构。

## 主要更新

- 从原生壳层修复 Windows 早期版本遗留 Service Worker 继续提供旧界面、导致按钮失效的问题；只清理旧静态外壳，不删除 IndexedDB 或 Local Storage。
- 电脑端练习与新建计划把分类、题型和标记收拢为紧凑范围摘要，并使用适合宽屏的居中选择窗口。
- 统一电脑端自主选题、计划设置、知识库和分类操作等弹窗的标题、滚动与操作区布局。
- 计划中心按最近实际学习时间排序；优化学习列表、长内容阅读、设置页和背诵／抽查结算页的信息层级与受控滚动。
- 保留首页计划轮播的电脑端左右箭头、全局搜索、四套主题与跟随系统、完整备份和知识库包等现有能力。

## 发布验证

- 当前全部 30 个 `.mjs` 自动化脚本通过，数据安全验收 18/18 通过，电脑端专项 14/14 通过。
- Windows 原生缓存自愈单元测试通过；使用旧坏 WebView 配置副本启动后，五项导航和设置恢复正常，16 条知识点仍可读取，6 个 IndexedDB 文件的长度与 SHA-256 均未改变。
- Windows 安装器文件版本和产品版本均为 0.95.0。
- Android 包名为 `com.rembry.app`，versionCode 为 95000，最低 Android 7.0，同时包含 arm64-v8a 与 armeabi-v7a，并使用既有永久证书签名。

覆盖安装前仍建议先在“设置 → 数据与备份”导出完整备份。Android 请直接覆盖安装，不要先卸载旧版。
