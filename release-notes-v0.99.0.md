# Rembry v0.99.0

本版本重点解决大型知识库进入缓慢、手机端长分类路径挤占阅读空间，以及删除知识库确认窗偏移的问题；继续保持本地数据和覆盖升级兼容。

## 主要优化

- 为知识库分类关系、各级数量和知识点顺序建立一次性运行时索引，减少大型资料反复全量扫描。
- 手机进入知识库时只加载当前层，不再同时生成不可见的电脑端整棵分类树和全部知识点列表。
- 长列表启用屏幕外绘制跳过，1152 条隔离测试资料进入当前层约 93ms。
- 手机知识点详情、专注阅读、背诵、抽查与计划阅读只简洁显示当前叶子分类，完整路径仍保留在辅助提示中。
- 优化“更多信息”标签与内容间距，并修复删除知识库确认窗跑到页面顶部的问题。
- 保留最多三级分类、安全导入导出、Windows 旧静态外壳自愈与 Android 永久签名。

## 发布验证

- 全部 33 个 `.mjs` 回归脚本通过；大型知识库专项 10/10、数据安全 19/19、手机长内容边界 20/20、三级分类 21/21、电脑端专项 14/14 通过。
- Windows 原生缓存自愈单元测试通过；覆盖安装不会清除 IndexedDB 或 Local Storage。
- Android 包名为 `com.rembry.app`，versionCode 为 99000，最低 Android 7.0，同时包含 arm64-v8a 与 armeabi-v7a，并继续使用既有永久证书签名。

覆盖安装前仍建议先在“设置 → 数据与备份”导出完整备份。Android 请直接覆盖安装，不要先卸载旧版。
