# Rembry v1.0.1

本版本优化全局搜索结果的相关性排序，不改变搜索界面和原有搜索范围。

## 本次更新

- 题目与搜索词完全一致的知识点现在优先显示。
- 题目简称完全一致时紧随精确题目显示。
- 标题和简称的前缀／包含匹配优先于关键词、标签、提纲和正文匹配。
- 关键词、标签、提纲、完整答案、来源和备注继续参与搜索，不会因本次排序优化消失。
- 相关性相同时继续沿用知识点原有手动顺序，避免结果无故跳动。
- 题型筛选、停用过滤、回收站过滤及电脑／手机共用的搜索界面保持不变。

## 兼容与数据

- 永久应用标识和 Android 包名仍为 `com.rembry.app`。
- 数据库结构、知识库数据和导入导出规则没有变化。
- 正常覆盖安装不会主动删除已有知识库、计划、进度或学习记录；升级前仍建议导出一次完整备份。
- Android 请直接覆盖安装，不要先卸载旧版。

## 验证

- 当前 34 个自动化脚本全部通过。
- 全局搜索相关性专项 18/18，通过电脑端、手机端、字段权重、稳定排序、题型筛选、停用／回收站过滤和横向边界验证。
- 提纲分组专项 60/60、数据安全 19/19、三级分类 21/21、手机边界 20/20、电脑专项 14/14、大型知识库 10/10 继续通过。
- Windows 原生旧缓存自愈单元测试通过。
- Android APK 包含 `arm64-v8a` 与 `armeabi-v7a`，最低支持 Android 7.0（API 24），并沿用永久正式签名。

## 文件

- Windows x64 安装器：`Rembry_1.0.1_windows_x64_setup.exe`
- Android 正式 APK：`Rembry_1.0.1_android.apk`
- 校验文件：`SHA256SUMS.txt`
