# Rembry v0.93.0 稳定测试版

本版本继续保持本地优先、离线可用，并统一发布 Windows x64 安装器与 Android 正式 APK。

## 本次更新

- 新增奶油青、暖杏、雾蓝、夜墨四套主题，并支持跟随系统；Android 状态栏与底部系统栏同步主题颜色和图标明暗。
- 补齐夜墨主题的练习恢复卡、学习列表切换、关键词、拖拽浮层、危险操作和旧控件表面适配，同时保留其他主题原有表现。
- 优化手机端背诵与抽查本轮结算页：减少重复说明，压缩空白，强化主操作、重测与放弃操作层级。
- Android 完整备份与知识库包导出新增“保存到手机 / 分享给其他应用”选择；保存走系统文件窗口，分享走安卓系统分享面板。
- 手机端一级模块支持在页面顶部下拉刷新；深层页面、弹窗、学习进行页和搜索覆盖层不会误触发。
- 修复多处手机端滚动、水平划动边界、分页、安全区与设置深层页布局问题。

## 安装与数据

- Windows：运行 `Rembry_0.93.0_windows_x64_setup.exe`，可选择安装目录。
- Android：安装 `Rembry_0.93.0_android.apk`；请直接覆盖旧版，不要先卸载。
- 正常覆盖安装不会主动清除已有本地数据，但升级前仍建议在“设置 → 数据与备份”导出完整备份。
- Android 最低支持 Android 7.0，APK 包含 `arm64-v8a` 与 `armeabi-v7a`。

## 校验

下载后可使用一同提供的 `SHA256SUMS.txt` 校验文件完整性。
