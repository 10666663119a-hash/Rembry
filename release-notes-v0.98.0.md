# Rembry v0.98.0

本版本在 0.97.0 稳定基线上修复手机端新建知识点页的横向间距与 Android 点击高亮问题，并继续保持本地数据和覆盖升级兼容。

## 主要修复

- 修复手机端新建知识点内容面板左右留白不对称；现已统一为对称间距，不再出现右侧裁切。
- 去除 Android WebView 触控按钮时的蓝色点击框，同时保留键盘操作所需的 `focus-visible` 焦点反馈。
- 保留最多三级分类、安全导入导出、Windows 旧静态外壳自愈与 Android 永久签名。

## 发布验证

- 全部 31 个 `.mjs` 回归脚本通过，数据安全 19/19、手机端长内容边界 20/20、三级分类 21/21、电脑端专项 14/14 通过。
- Windows 原生缓存自愈单元测试通过；本机已原位覆盖升级至 0.98.0，升级前本地 IndexedDB 备份哈希一致。
- Android 包名为 `com.rembry.app`，versionCode 为 98000，最低 Android 7.0，同时包含 arm64-v8a 与 armeabi-v7a，并继续使用既有永久证书签名。

覆盖安装前仍建议先在“设置 → 数据与备份”导出完整备份。Android 请直接覆盖安装，不要先卸载旧版。
