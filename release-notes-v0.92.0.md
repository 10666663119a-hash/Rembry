# Rembry v0.92.0 稳定测试版

发布日期：2026年8月12日

v0.92.0继续收紧手机端长列表、滑动与安全区表现。Windows与Android使用同一份业务资源和本地数据结构，可以在旧版基础上直接覆盖安装。

## 主要更新

- 手机知识库总览保留每页5项；进入知识库后的分类和知识点取消分页，改为固定区域内滚动，继续支持一级／二级分类及知识点长按拖动排序。
- 手机计划学习列表固定标题、未完成／已完成和轮次信息，只滚动下方知识点列表；超过上限后分页位于列表自然底部。
- 背诵、知识库详情和计划学习详情统一为平滑水平划动，第一条与最后一条越界时不再产生无效回弹动画。
- 手机设置详细页改为全宽轻量标题栏，去除两侧留白和方框感。
- 修复Android新建知识点与首页选择计划重复叠加顶部安全区导致的整体下移。

## 稳定性检查

- 发布前完整执行28个自动化脚本，全部通过；覆盖手机触摸拖动、边缘滚动、划动边界、安全区、PWA离线和双端基础功能。
- Windows安装器和Android正式APK来自同一份0.92.0源码与正式资源标识`092-rembry-release`。
- Android版本号为92000，包含`arm64-v8a`与`armeabi-v7a`，最低支持Android 7.0，并继续使用既有永久证书签名。

## 安装与数据

- Windows提供x64 NSIS安装器，可选择安装目录。
- Android请直接覆盖安装，不要先卸载旧版；卸载会删除该应用的本地数据。
- 本版本没有提升数据结构版本，覆盖安装不会主动清除本地知识库、设置或计划数据。
- 升级前仍建议在“设置 → 数据与备份”导出完整备份。
- 安装包完整性请使用同一Release中的`SHA256SUMS.txt`校验。
