# Rembry v0.96.0

本版本在 0.95.0 稳定基线上完善知识库分类能力，并补齐资料代录工作流。

## 主要更新

- 分类层级由最多二级扩展为最多三级，一级和二级分类详情均可直接新建下级分类，并自动带入当前上级。
- 手机端与电脑端知识库树、移动分类、移动知识点、练习范围、新建计划、自主选题、专注阅读目录、搜索与筛选等关联流程均适配三级分类。
- 延续“目录不直接存放知识点”的规则；三级分类为最深层级，导入文件若包含第四级会在写入前明确拦截。
- 知识库包与完整备份可保留三级父子关系；新增资料代录专用规范、模板和离线校验工具，便于由独立 Codex 对话安全生成可导入资料包。

## 发布验证

- 全部 30 个现有 `.mjs` 回归脚本通过，三级分类专项 21/21 通过，数据安全验收 19/19 通过。
- Windows 原生缓存自愈单元测试通过；自愈仍只处理旧静态外壳，不清除 IndexedDB 或 Local Storage。
- Android 包名为 `com.rembry.app`，versionCode 为 96000，最低 Android 7.0，同时包含 arm64-v8a 与 armeabi-v7a，并继续使用既有永久证书签名。

覆盖安装前仍建议先在“设置 → 数据与备份”导出完整备份。Android 请直接覆盖安装，不要先卸载旧版。
